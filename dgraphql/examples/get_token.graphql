query($address: String!){
  token(address: $address) {
    cursor
    node {
      address
      mintAuthority
      freezeAuthority
      supply
      decimals
      verified
      meta {
        name  
        symbol
        logo
        website
      }
    }
  }
}
