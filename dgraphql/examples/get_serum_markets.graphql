{
  serumMarkets(count: 100) {
    pageInfo {
      startCursor
      endCursor
      hasNextPage
    }
    edges {
      cursor
      node {
        address
        name
        baseToken {
          address
          decimals
          supply
          meta {
            name
            symbol
          }
        }
        quoteToken {
          address
          decimals
          supply
          meta {
            name
            symbol
          }
        }
      }
    }
  }
}
