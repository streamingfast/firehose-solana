type Token {
  address: String!
  name: String
  symbol: String
  mintAuthority: String!
  freezeAuthority: String!
  supply: Uint64!
  decimals: Int!
}

type TokenResponse {
  address: String!
  mintAuthority: String!
  freezeAuthority: String!
  supply: Uint64!
  decimals: Int!
}
